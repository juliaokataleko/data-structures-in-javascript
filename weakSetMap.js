/**
 * WeakSet and SetMap
 */

// Variations of set and map
// values and keys are only "weakly referenced"

// Garbage collection can delete values and 
// keys if not used anywhere else in the app